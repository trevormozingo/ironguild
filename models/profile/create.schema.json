{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "CreateProfile",
  "description": "Schema for creating a new profile. Server generates id, timestamps, and defaults.",
  "type": "object",
  "properties": {
    "username": {
      "$ref": "base.schema.json#/properties/username"
    }
  },
  "required": ["username"],
  "additionalProperties": false
}
