{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "RSVPEvent",
  "description": "Schema for updating RSVP status on an event.",
  "type": "object",
  "properties": {
    "status": {
      "type": "string",
      "enum": ["accepted", "declined"],
      "description": "RSVP status (pending is set by server only)"
    }
  },
  "required": ["status"],
  "additionalProperties": false
}
