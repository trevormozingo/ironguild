{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "CommentResponse",
  "description": "API response shape for a comment. Extends base with resolved fields.",
  "type": "object",
  "allOf": [
    { "$ref": "base.schema.json" }
  ],
  "properties": {
    "authorUsername": {
      "type": "string",
      "description": "Username of the comment author (resolved at query time)"
    },
    "authorProfilePhoto": {
      "type": ["string", "null"],
      "format": "uri",
      "description": "Profile photo URL of the comment author (resolved at query time)"
    }
  }
}
