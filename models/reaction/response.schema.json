{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ReactionResponse",
  "description": "API response shape for a reaction. Extends base with resolved fields.",
  "type": "object",
  "allOf": [
    { "$ref": "base.schema.json" }
  ],
  "properties": {
    "username": {
      "type": "string",
      "description": "Username of the user who reacted (resolved at query time)"
    },
    "profilePhoto": {
      "type": ["string", "null"],
      "format": "uri",
      "description": "Profile photo URL of the user who reacted (resolved at query time)"
    }
  }
}
